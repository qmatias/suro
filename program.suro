{
    set var to true;
    set other to false;

    -- make sure var is false
    if var then {
        change var to false;
    };

    if var then {
        call print with ("var is true");
    } else if other then {
        call print with ("other is true");
    } else {
        call print with ("Neither var nor other are true");
    };
}