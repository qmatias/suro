{
    set var to true;

    -- make sure var is false
    if var then {
        change var to false;
    };

    if var then {
        call print with ("Var is true");
    } else {
        call print with ("Var is false");
    };
}